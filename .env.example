# Base URL for the Blazar contact-form API.
# Production default (used when this var is unset) is https://api.cloud-lord.com
# For local development against a Blazar instance running on your machine:
#   VITE_API_BASE=http://localhost:3030
VITE_API_BASE=https://api.cloud-lord.com
